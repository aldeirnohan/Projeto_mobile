<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projeto_mobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projeto_mobile">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login"></activity>
        <activity android:name=".IndexActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".UpdateImovelActivity" />
        <activity android:name=".ViewImovelActivity" />
        <activity android:name=".CadastroImovelActivity" />
        <activity android:name=".UpdateUserActivity" />
        <activity android:name=".ProfileUserActivity" />
        <activity android:name=".CadastroUserActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>